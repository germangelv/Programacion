#include "funciones.h"

void acumular ( t_info *a, const t_info *b)
{
    //a->importe+=b->importe;
}
int comparar (const t_info *a, const t_info *b)
{
    return a->cla - b->cla;
    /*
    if (a->cla == b->cla )
    //if( a->dni == b->dni )
       return 0;
    if (a->cla > b->cla )
    //if( a->dni == b->dni )
       return 1;
    else
    //if( a->dni == b->dni )
       return -1;*/
    ///terminar
}
